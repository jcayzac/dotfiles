[ "$(uname -s)" == "Darwin" ] || return

append-path '/opt/local/bin' '/opt/local/sbin'
export MANPATH="/opt/local/share/man:${MANPATH}"
alias port='sudo port'

# ex: noet ci pi sts=0 sw=4 ts=4 filetype=sh
